@model MediaRequest.WebUI.ViewModels.ActorViewModel

<div class="container actor-container">
    <h1 class="actor-name">@Model.Actor.name</h1>
    <div class="actor-head">
        <img src="@Model.Actor.profile_path" />
        <div class="actor-info">
            <pre>@Model.Actor.biography</pre>
        </div>
    </div>
    <div class="actor-body">
        @*@foreach (var item in Model.Movies.cast)
        {
            @await Component.InvokeAsync("MovieCard", new { maxPriority = 1, isDone = false, movieId = item.id.ToString() });
        }*@
        <div class="request-container">
            @for (int i = 1; i <= 3; i++)
            {
                @await Component.InvokeAsync("MovieCard", new { maxPriority = 1, isDone = false, movieId = Model.Movies.cast[i].id.ToString() });
            }
        </div>
    </div>
</div>
